pluginManagement {
    repositories {
        maven { url = uri("https://repo.spring.io/release") }
        maven { url = uri("https://repo.maven.apache.org/maven2") }
        gradlePluginPortal()
    }
}
rootProject.name = "accesstosql"
