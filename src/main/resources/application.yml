spring:
  jpa:
    database-platform: org.hibernate.dialect.SQLServerDialect

  datasource:
    jdbc-url: jdbc:sqlserver://localhost:1433;database=ArchiveFund
    username: sa
    password: MiKe_5526

  access-datasource:
    jdbc-url: jdbc:ucanaccess://G:\test.mdb;memory=false;openExclusive=true;ignoreCase=true

  main:
    banner-mode: off

  batch:
    job:
      enabled: false
    jdbc:
      initialize-schema: always
